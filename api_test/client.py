import requests
import json
 url = 'http://localhost:5000'
  data.
  data = requests.get(url)
  if data.status_code == 200:
     data = data.json()

    for e in data[payload]
         print(e['payload'])
      


